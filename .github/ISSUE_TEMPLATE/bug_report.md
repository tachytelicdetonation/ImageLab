---
name: Bug report
about: Something broke — a crash, a wrong number, a command that doesn't do what the docs say
labels: bug
---

**What happened**

**Repro** (the smallest config + command; `--tier smoke` if it reproduces there)

```
lab run ... --tier smoke
```

**Expected**

**Environment** (`python -V`, `python -c "import torch; print(torch.__version__)"`, OS)
